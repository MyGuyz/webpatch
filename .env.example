# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
#
# ถ้าไม่ใส่ เว็บจะรันในโหมดตัวอย่างโดยใช้ข้อมูลสมมติ (src/lib/seed-games.js)
# ซึ่งใช้พัฒนาหน้าเว็บได้โดยไม่ต้องมีบัญชี Supabase

# หาได้จาก Supabase → Project Settings → API
# สองค่านี้เปิดเผยได้ ความปลอดภัยอยู่ที่ RLS ในไฟล์ supabase/migrations/
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
